apiVersion: v2
name: langfuse
description: Langfuse LLM observability (upstream chart vendored offline, self-contained DB stack).
type: application
version: 0.1.0
appVersion: "3.185.0"
dependencies:
  - name: langfuse
    version: 1.5.35
    repository: https://langfuse.github.io/langfuse-k8s
