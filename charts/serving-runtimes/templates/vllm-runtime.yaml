---
# ConfigMap with fallback chat templates. Used by the vLLM runtime when the
# upstream model's tokenizer does not define one (transformers >=4.44 stopped
# allowing a default template). The startup script picks a template only when
# the tokenizer is missing it — models that ship their own (TinyLlama, Qwen
# Instruct, etc.) are left untouched.
apiVersion: v1
kind: ConfigMap
metadata:
  name: vllm-chat-templates
  namespace: {{ .Values.namespace }}
data:
  # Generic ChatML — works for Qwen-style models and is a reasonable default
  # for any modern instruct model that lost its template during quantization.
  # The block is wrapped in a Helm raw-literal so Helm does not interpret the
  # Jinja double-brace placeholders as Go template actions.
  chatml.jinja: |
{{ `    {% for message in messages -%}
    {% if message['role'] == 'system' -%}
    <|im_start|>system
    {{ message['content'] }}<|im_end|>
    {% elif message['role'] == 'user' -%}
    <|im_start|>user
    {{ message['content'] }}<|im_end|>
    {% elif message['role'] == 'assistant' -%}
    <|im_start|>assistant
    {{ message['content'] }}<|im_end|>
    {% endif -%}
    {% endfor -%}
    {% if add_generation_prompt -%}
    <|im_start|>assistant
    {% endif -%}` }}
---
# vLLM ClusterServingRuntime
#
# Critical design choices:
# 1. schedulerName: hami-scheduler — REQUIRED for HAMi vRAM partitioning to work.
#    Without this, default-scheduler runs the pod and HAMi accounting is bypassed.
# 2. Resource limits here are DEFAULTS — KRO Template overrides them per model
#    via InferenceService.spec.predictor.model.resources.
# 3. We DON'T use KServe's storage initializer (modelcar/PVC download). vLLM downloads
#    directly from HuggingFace at runtime — simpler for PoC. For production, swap to
#    OCI modelcar or pre-baked PVC.
# 4. Env vars (MODEL_ID, MAX_MODEL_LEN etc) are templated into args. Overridden per
#    InferenceService via env injection by KRO.
# 5. Chat-template fallback: some HF quants ship without tokenizer_config.json
#    chat_template (e.g. Qwen/Qwen2.5-0.5B-Instruct-AWQ). The startup script
#    probes the tokenizer and only passes --chat-template when missing, so it
#    never overrides templates that the model already provides.
apiVersion: serving.kserve.io/v1alpha1
kind: ClusterServingRuntime
metadata:
  name: vllm-runtime
spec:
  annotations:
    prometheus.kserve.io/path: /metrics
    prometheus.kserve.io/port: "8000"

  supportedModelFormats:
    - name: vllm
      version: "1"
      autoSelect: true
      priority: 1

  protocolVersions:
    - v2

  # Force pod onto HAMi scheduler — critical for vRAM partitioning
  schedulerName: hami-scheduler

  containers:
    - name: kserve-container
      image: {{ .Values.vllm.image }}
      command:
        - /bin/sh
        - -c
      args:
        - |
          set -e

          if ! python3 -c "import opentelemetry.sdk, opentelemetry.exporter.otlp.proto.grpc.trace_exporter, opentelemetry.semconv_ai" >/dev/null 2>&1; then
            echo "Installing OpenTelemetry packages required by vLLM tracing..."
            python3 -m pip install --no-cache-dir \
              "opentelemetry-api>=1.26.0,<1.27.0" \
              "opentelemetry-sdk>=1.26.0,<1.27.0" \
              "opentelemetry-exporter-otlp>=1.26.0,<1.27.0" \
              "opentelemetry-semantic-conventions-ai>=0.4.1,<0.5.0"
          fi

          # Chat-template fallback: pass --chat-template only when the
          # tokenizer is missing one (transformers >=4.44 no longer supplies
          # a default). Models that already ship a template keep theirs.
          CHAT_TEMPLATE_ARG=""
          if python3 - <<'PY'
          import os, sys
          try:
              from transformers import AutoTokenizer
              t = AutoTokenizer.from_pretrained(
                  os.environ["MODEL_ID"], trust_remote_code=True
              )
              sys.exit(0 if t.chat_template else 1)
          except Exception as e:
              print(f"chat-template probe failed: {e}", file=sys.stderr)
              sys.exit(1)
          PY
          then
              echo "Tokenizer ships a chat_template — using model's own."
          else
              echo "Tokenizer has no chat_template — falling back to /etc/vllm/chatml.jinja"
              CHAT_TEMPLATE_ARG="--chat-template=/etc/vllm/chatml.jinja"
          fi

          exec python3 -m vllm.entrypoints.openai.api_server \
            --port=8000 \
            --host=0.0.0.0 \
            --model="$${MODEL_ID}" \
            --served-model-name="$${SERVED_NAME}" \
            --quantization="$${QUANTIZATION}" \
            --max-model-len="$${MAX_MODEL_LEN}" \
            --gpu-memory-utilization="$${GPU_MEM_UTIL}" \
            --tensor-parallel-size="$${TENSOR_PARALLEL_SIZE}" \
            --dtype=auto \
            --trust-remote-code \
            --enable-prefix-caching \
            --disable-log-requests \
            --otlp-traces-endpoint="$${OTEL_EXPORTER_OTLP_ENDPOINT}" \
            --collect-detailed-traces=all \
            $CHAT_TEMPLATE_ARG

      ports:
        - containerPort: 8000
          protocol: TCP
          name: http

      env:
        # Defaults — overridden per-InferenceService by KRO template
        - name: MODEL_ID
          value: "google/gemma-3-1b-it"
        - name: SERVED_NAME
          value: "default"
        - name: QUANTIZATION
          value: "awq"
        - name: MAX_MODEL_LEN
          value: "2048"
        - name: GPU_MEM_UTIL
          value: "0.85"
        # Number of GPU cards for tensor parallelism. Default 1 (single GPU).
        # KRO overrides this per-InferenceService from spec.gpuCards, and must
        # match the nvidia.com/gpu count requested for the pod.
        - name: TENSOR_PARALLEL_SIZE
          value: "1"
        - name: HF_HOME
          value: /models/.cache/huggingface
        - name: HUGGING_FACE_HUB_TOKEN
          valueFrom:
            secretKeyRef:
              name: huggingface-token
              key: token
              optional: true
        - name: VLLM_LOGGING_LEVEL
          value: INFO
        - name: NCCL_DEBUG
          value: WARN
        # ─── OpenTelemetry tracing config ─────────────────────────────────
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: "http://otel-collector.ai-platform.svc.cluster.local:4317"
        - name: OTEL_EXPORTER_OTLP_PROTOCOL
          value: "grpc"
        - name: OTEL_EXPORTER_OTLP_INSECURE
          value: "true"
        - name: OTEL_SERVICE_NAME
          value: "vllm-inference"
        - name: OTEL_RESOURCE_ATTRIBUTES
          value: "service.namespace=inference,deployment.environment=poc"
        - name: OTEL_TRACES_SAMPLER
          value: "always_on"
        - name: K8S_POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: K8S_NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName

      resources:
        # Defaults — KRO overrides via InferenceService.spec.predictor.model.resources
        requests:
          cpu: "2"
          memory: 8Gi
          nvidia.com/gpu: 1
          nvidia.com/gpumem-percentage: "45"
          nvidia.com/gpucores: "45"
        limits:
          cpu: "4"
          memory: 16Gi
          nvidia.com/gpu: 1
          nvidia.com/gpumem-percentage: "45"
          nvidia.com/gpucores: "45"

      volumeMounts:
        - name: model-cache
          mountPath: /models
        - name: shm
          mountPath: /dev/shm
        - name: vllm-chat-templates
          mountPath: /etc/vllm
          readOnly: true

      livenessProbe:
        httpGet:
          path: /health
          port: 8000
        initialDelaySeconds: 600
        periodSeconds: 30
        timeoutSeconds: 10
        failureThreshold: 5

      readinessProbe:
        httpGet:
          path: /health
          port: 8000
        initialDelaySeconds: 120
        periodSeconds: 10
        timeoutSeconds: 5
        failureThreshold: 30

      startupProbe:
        httpGet:
          path: /health
          port: 8000
        initialDelaySeconds: 60
        periodSeconds: 15
        failureThreshold: 80   # Up to ~20min for cold model download
        timeoutSeconds: 10

  volumes:
    - name: model-cache
      persistentVolumeClaim:
        claimName: model-cache-pvc
    - name: shm
      emptyDir:
        medium: Memory
        sizeLimit: 2Gi
    - name: vllm-chat-templates
      configMap:
        name: vllm-chat-templates
        items:
          - key: chatml.jinja
            path: chatml.jinja
